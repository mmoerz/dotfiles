# My dotfiles

This is my repository for .-files that I utilize on linux machines.

## Installation
 simply checkout to .dotfiles in your home directory and then run
``` .dotfiles/script/bootstrap ```

## Cargo specific binary unix tools
``` sudo apt-get install duf ```

cargo install exa
cargo install procs
cargo install ripgrep
cargo install du-dust
cargo install tldf 
